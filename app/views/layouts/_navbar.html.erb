<header class="fixed top-0 w-full">
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-16">
  <div class="container-fluid">
     <%= link_to 'Recipe App', root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav w-full me-auto mb-2 mb-lg-0 flex justify-between bg-blue">
        <% if user_signed_in? %>
        <span class="flex justify-center align-center">
          <li class="nav-item">
            <%= link_to 'Foods', foods_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Add Food', new_food_path, class: "nav-link" %>
          </li>
          <span class="w-auto p-2">
            <%= button_to "Log Out", destroy_user_session_path, method: :delete, data: { turbo: false }, class:"btn btn-primary" %>
          </span>
        </span>
          <% else %>
          <span class="container w-full flex">
            <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %>
          </span>
        <% end %>
       </ul>
    </div>
  </div>
</nav>
</header>
