<%= stylesheet_link_tag('humb') %>
<nav class="navbar navbar-expand-lg p-3 mb-2 bg-success text-dark w-100">
  <div class="container flex flex-wrap justify-between align-items-center mx-auto items-center">
     <%= link_to 'Recipe App', root_path, class: "text-xl text-decoration-none text-warning fs-3" %>
     <button class="navbar-toggler text-warning" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
     <%= image_tag('humb.png', class:"navbar-toggler-icon humb") %>
   </button>
   <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="d-flex gap-4 justify-content-center align-items-center pt-2 navbar-nav">
        <% if user_signed_in? %>
            <li class="nav-item">
                <%= link_to 'Foods', foods_path, class: "text-decoration-none text-white text-sm sm:text-xs hover:text-black nav-link active" %>
            </li>
            <li class="nav-item">
                <%= link_to 'PublicRecipe', public_recipes_path, class: "text-decoration-none text-white text-sm sm:text-xs nav-link active" %>
            </li>
            <li class="nav-item">
                <%= link_to 'Inventories', user_inventories_path(user_id: current_user), class: "text-decoration-none text-white text-sm sm:text-xs nav-link active" %>
            </li>
            <li class="nav-item">
                <%= link_to 'shoppinglist', shopping_list_index_path, class: "text-decoration-none text-white text-sm sm:text-xs nav-link active" %>
            </li>
            <%= button_to "Log Out", destroy_user_session_path, method: :delete, data: { turbo: false }, class:"btn btn-danger" %>
          <% else %>
          <div class="d-flex gap-4">
            <%= link_to "Sign in", new_user_session_path, class: "text-decoration-none text-white" %>
            <%= link_to "Sign up", new_user_registration_path, class: "text-decoration-none text-white" %>
          </div>
        <% end %>
       </ul>
       </div>
    </diV>
</nav>
